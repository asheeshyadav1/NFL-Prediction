.venv/
__pycache__/
*.pyc
.pytest_cache/
# data/raw/ is deliberately NOT ignored: Dockerfile.model bakes the cached
# parquet in (~8 MB) so the model service builds its features offline. Without
# it the container downloads at startup and dies on readOnlyRootFilesystem.
.git/
.github/
# Build output only -- frontend/ itself must stay in the context for
# Dockerfile.frontend.
node_modules/
.next/
